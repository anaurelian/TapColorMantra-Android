<?xml version="1.0" encoding="utf-8"?>
<!--
    Copyright (C) 2014-2016 AnAurelian. All rights reserved.
    https://anaurelian.com
-->
<resources>

    <!-- ************** -->
    <!-- App Theme      -->
    <!-- ************** -->

    <!-- Base application theme -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <item name="android:windowFullscreen">true</item>
        <item name="android:actionBarStyle">@style/AppActionBarStyle</item>
        <item name="android:actionOverflowButtonStyle">@style/AppActionBarOverflow</item>
        <item name="android:windowContentOverlay">@null</item>

        <item name="actionBarCorrection">?android:attr/actionBarSize</item>
    </style>

    <!-- Customized background Action Bar style -->
    <style name="AppActionBarStyle" parent="android:Widget.Holo.ActionBar">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:displayOptions">none</item>
    </style>

    <!-- Action Bar material Overflow icon style -->
    <style name="AppActionBarOverflow" parent="@android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_more_vert_white_shadow</item>
    </style>

    <!-- ************** -->
    <!-- Settings Theme -->
    <!-- ************** -->

    <!-- Settings activity theme -->
    <style name="AppSettingsTheme" parent="android:Theme.Holo.Light.DialogWhenLarge">
        <item name="android:windowFullscreen">true</item>
        <item name="android:actionBarStyle">@style/AppSettingsActionBarStyle</item>
        <item name="android:homeAsUpIndicator">@drawable/up_indicator</item>
    </style>

    <!-- Customized background Action Bar style -->
    <style name="AppSettingsActionBarStyle" parent="android:Widget.Holo.ActionBar">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:displayOptions">showTitle|homeAsUp</item>
    </style>

</resources>